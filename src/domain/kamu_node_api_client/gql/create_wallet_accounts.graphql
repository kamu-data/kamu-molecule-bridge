mutation CreateWalletAccounts($newWalletAccounts: [DidPkh!]!) {
  accounts {
    createWalletAccounts(walletAddresses: $newWalletAccounts) {
      __typename
      ... on CreateWalletAccountsSuccess {
        message
        accounts {
          id
        }
      }
    }
  }
}
